// Rivm Bagdasev — Art Direction
// No interactive logic currently required — page uses native HTML/CSS only
// (video autoplay via HTML attributes, sticky header via CSS).
// This file is kept as a placeholder for future JS additions.

document.addEventListener('DOMContentLoaded', function () {
  // Ready for future enhancements.
});